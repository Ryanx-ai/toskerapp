# Managed Postgres (server only). Use separate Neon branches/databases per environment.
DATABASE_URL=

# Clerk (MS5.1; documented now, not consumed by the application yet).
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
